version: '2'
services:
  certbot_img:
    build: .
    ports:
      - "80:80"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /mnt:/mnt
      - /etc/letsencrypt/:/etc/letsencrypt/
      - /var/log/docker_log:/var/log/docker_log
    image: certbot
    container_name: certbot
