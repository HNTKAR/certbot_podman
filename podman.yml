# Generation of Kubernetes YAML is still under development!
#
# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-1.6.4
apiVersion: v1
kind: Pod
metadata:
  labels:
    app: certbot_pod
  name: certbot_pod
spec:
  containers:
  - env:
    - name: HOSTNAME
      value: certbot_pod
    - name: container
      value: podman
    image: localhost/certbot:latest
    name: certbot
    ports:
    - containerPort: 80
      hostPort: 1080
      protocol: TCP
    resources: {}
    securityContext:
      allowPrivilegeEscalation: true
      capabilities: {}
      privileged: false
      readOnlyRootFilesystem: false
    tty: true
    volumeMounts:
    - mountPath: /etc/letsencrypt/
      name: certbot-volume
    - mountPath: /var/log/
      name: certbot-volume-2
    workingDir: /
  volumes:
  - hostPath:
      path: /home/podman/certbot_pod/letsencrypt/
      type: Directory
    name: certbot-volume
  - hostPath:
      path: /home/podman/certbot_pod/log/
      type: Directory
    name: certbot-volume-2
status: {}

